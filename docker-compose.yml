services:
  puppies:
    build: puppy
    ports:
      - "127.0.0.1:2000-2030:80"
    stop_signal: SIGINT
    volumes:
      - '/dev/shm/mgx-temp:/tmpfs'
    deploy:
      mode: replicated
      replicas: 30
      endpoint_mode: vip
